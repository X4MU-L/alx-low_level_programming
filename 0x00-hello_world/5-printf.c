#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints to stdout using printf
 * Return: Always 0 (Success)
 */
int main(void)
{
	char art[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", art);
	return (0);
}
