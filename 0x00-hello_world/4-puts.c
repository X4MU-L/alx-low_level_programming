#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints to the stdout using puts
 * Return: Always 0 (Success)
 */
int main(void)
{
	char my_prnt[] = "\"Programming is like building a multilingual puzzle";

	puts(my_prnt);
	return (0);
}
