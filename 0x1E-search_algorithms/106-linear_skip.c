#include "search_algos.h"

/**
 * linear_skip - implement a linear skip search
 *
 * @list: - the linked list to search for @value
 * @value: - the value to search for
 * Return: - returnsthe node of value if found else NULL
 */
skiplist_t *linear_skip(skiplist_t *list, int value)
{

	if (!list || value <= 0)
		return (NULL);

	return (list);
}
